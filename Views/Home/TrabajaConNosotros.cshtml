@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Trabaja con Nosotros";
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Trabaja con Nosotros</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <style>
        :root {
            --bg-gradient: linear-gradient(135deg, #1e3c72, #2a5298);
            --primary-color: #90caf9;
            --accent-color: #ff7f50;
            --highlight-color: #ff6347;
            --shadow-color: rgba(0,0,0,0.4);
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: var(--bg-gradient);
            color: var(--primary-color);
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 4rem auto;
            padding: 2rem;
            background: rgba(0, 0, 0, 0.55);
            border-radius: 15px;
            box-shadow: 0 8px 30px var(--shadow-color);
            text-align: center;
        }

        h2 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }

        .btn-apply {
            padding: 1rem 2rem;
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--primary-color);
            background: linear-gradient(90deg, var(--accent-color), var(--highlight-color));
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: transform 0.3s, box-shadow 0.3s;
            box-shadow: 0 5px 15px var(--shadow-color);
            text-decoration: none;
            display: inline-block;
        }

            .btn-apply:hover {
                transform: scale(1.05);
                box-shadow: 0 8px 20px var(--shadow-color);
            }
    </style>
</head>
<body>
    <div class="container">
        <h2>Trabaja con Nosotros</h2>
        <p>Bienvenido a nuestra sección de "Trabaja con Nosotros". Si deseas unirte a nuestro equipo, haz clic en el botón de abajo para llenar la solicitud.</p>
        <a asp-controller="TrabajaConNosotros" asp-action="Create" class="btn-apply">Llenar Solicitud</a>
    </div>

    <script>
        // Animación de entrada para la vista
        gsap.from(".container", { duration: 1.5, y: 50, opacity: 0, ease: "power3.out" });

        // Animación para el botón al hacer hover
        document.querySelector('.btn-apply').addEventListener('mouseenter', () => {
            gsap.to('.btn-apply', { scale: 1.08, duration: 0.3, ease: "power2.out" });
        });
        document.querySelector('.btn-apply').addEventListener('mouseleave', () => {
            gsap.to('.btn-apply', { scale: 1, duration: 0.3, ease: "power2.out" });
        });
    </script>
</body>
</html>
