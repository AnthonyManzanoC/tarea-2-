@{
    Layout = null; // Asegura que la vista sea independiente
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adiós</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="~/css/site.css"> <!-- Ajusta según tu estructura -->

    <style>
        /* Contenedor principal */
        .goodbye-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #f0f0f0, #e6e6e6);
        }

        /* Tarjeta de despedida */
        .goodbye-card {
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15);
            text-align: center;
            max-width: 500px;
            font-family: 'Poppins', sans-serif;
            animation: fadeIn 0.8s ease-in-out;
        }

        /* Ícono */
        .icon-success {
            font-size: 60px;
            color: #27ae60;
            margin-bottom: 15px;
        }

        /* Botón */
        .btn-return {
            display: inline-block;
            padding: 12px 24px;
            border-radius: 8px;
            background: linear-gradient(45deg, #1e3c72, #2a5298);
            color: white;
            font-weight: bold;
            text-decoration: none;
            text-transform: uppercase;
            transition: all 0.3s ease-in-out;
        }

            .btn-return:hover {
                transform: scale(1.05);
                box-shadow: 0 4px 10px rgba(42, 82, 152, 0.4);
            }

        /* Animación de aparición */
        @@keyframes fadeIn {
            from

        {
            opacity: 0;
            transform: translateY(-20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }
    </style>
</head>
<body>
    <div class="goodbye-container">
        <div class="goodbye-card">
            <i class="fas fa-check-circle icon-success"></i>
            <h2>Cuenta Eliminada con Éxito</h2>
            <p>Gracias por haber sido parte de nuestra comunidad. Si alguna vez deseas regresar, estaremos aquí para ti.</p>
            <p>Si necesitas asistencia, no dudes en contactarnos.</p>
            <a href="/" class="btn-return">Volver al Inicio</a>
        </div>
    </div>
</body>
</html>
